# CPU-only PyTorch for chess image recognition.
# Recommended for servers/VMs without a GPU.

--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.12.1+cpu
torchvision==0.27.1+cpu

-r requirements-chess.txt
